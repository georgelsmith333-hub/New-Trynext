name: Active app verification

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: active-app-verification-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Typecheck, test, and build active apps
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared TypeScript declarations
        run: pnpm run typecheck:libs

      - name: Typecheck storefront
        run: pnpm --filter @workspace/trynext-storefront typecheck

      - name: Validate active Smart Object runtime
        run: pnpm run validate:mockups

      - name: Test storefront
        run: pnpm --filter @workspace/trynext-storefront test

      - name: Typecheck API
        run: pnpm --filter @workspace/api-server typecheck

      - name: Run API tests
        run: pnpm --filter @workspace/api-server test

      - name: Build API bundle
        run: pnpm --filter @workspace/api-server build

      - name: Build storefront
        run: pnpm --filter @workspace/trynext-storefront build
